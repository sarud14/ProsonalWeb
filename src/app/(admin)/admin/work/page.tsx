import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Manage Work — Admin — FEOps Kit',
}

export default function AdminWorkPage(): React.JSX.Element {
  return (
    <main>
      <h1>Manage Work</h1>
    </main>
  )
}
