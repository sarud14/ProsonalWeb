import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Settings — Admin — FEOps Kit',
}

export default function AdminSettingsPage(): React.JSX.Element {
  return (
    <main>
      <h1>Settings</h1>
    </main>
  )
}
